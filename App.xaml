<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="Shinobu.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Shinobu">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>
            <local:BoolToStringConverter x:Key="BoolToStringConverter" TrueValue="★" FalseValue="☆" />
            <local:FilenameFromPathConverter x:Key="FilenameFromPathConverter"/>
            <local:FileSizeConverter x:Key="FileSizeConverter" />
            <FontFamily x:Key="JapaneseFont">Yu Gothic UI, Meiryo, Segoe UI, sans-serif</FontFamily>
            <Style x:Key="RadioButtonWithPreview" TargetType="RadioButton" BasedOn="{StaticResource DefaultRadioButtonStyle}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
